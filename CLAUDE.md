
# 视频理解创新点实现方式仓库

本文件为 coding agent 专用规则文件，如项目有更新，则必须同步更新此文件内容. 你可以阅读 docs文件夹 - 了解本项目的创新点的具体方式以及打算实现的方式



## 开发范式

### 中文文档

所有注释与文档都应该由中文作为主要语言编写，对于出现全英文注释/文档，提醒用户做出 “翻译为中文” 的指令.


### 包管理

项目依赖声明在 `pyproject.toml` 中，所有依赖必须指定确切版本号（如 `numpy==1.24.0`），不允许省略版本号或使用范围运算符（`>`, `<`, `>=`, `~=`）。

不要在文档中显示指定具体的 conda 环境名称， 环境由用户指定


### 文档管理

所有的模块开发应该有对应的文档记录在 docs 下. 如果你是人工智能代码工具, 请**在开发前阅读对应的文档**.在**开发后同步对应的文档内容**. 并**提醒使用者审核文档**.

每个知识点只允许有**一个 owner 文件**, 其他位置只写一行交叉链接, 不复述内容.
新增文档前先在 `docs/README.md` 的索引表登记归属. 不要把「本次修复」「变更点」
这类时点性记录写进参考文档, 实验结果以 `experiments/<name>/summary.json` 为准.


### 类型管理

所有 Python 代码都应保持清晰、一致且可维护的类型注释习惯。

要求：

- 为函数参数、返回值、类属性添加明确类型标注
- 为核心数据结构（config、dataset、batch、model input/output、pipeline 数据等）建立清晰类型定义
- 避免滥用 `Any`
- 优先使用 `dataclass`、`TypedDict`、枚举等方式描述结构化数据
- 类型注释应以提升可读性与可维护性为目标，避免过度复杂化
- 实验性代码可适当放宽，但核心训练、推理与数据处理流程应尽量保持完整类型信息

**在必要时, 将自定义类型写在 src/types 下.**



## 提交规范


- 提交信息使用 Angular Commit Message 规范。
- 提交信息允许中文或英文，但同一个提交内应保持语言一致。
- 推荐格式：

```text
<type>(<scope>, 可选): <subject>
```

- 常用 `type`：
  - `feat`: 新功能
  - `fix`: 缺陷修复
  - `docs`: 文档变更
  - `style`: 仅格式变更，不影响逻辑
  - `refactor`: 重构，不新增功能或修复缺陷
  - `perf`: 性能优化
  - `test`: 测试相关
  - `build`: 构建系统或依赖相关
  - `ci`: CI 配置相关
  - `chore`: 维护性变更
  - `revert`: 回滚提交

- 示例：

```text
feat: add ff3 method implentment
fix(scripts): 修复获取数据脚本卡住的问题
docs: 添加 ff3 方法文档
```

- 一个提交只包含一个明确目的。
- 不将无关格式化、重构和功能改动混入同一提交。
- 创建提交前应检查 `git status`，确认没有纳入无关文件。

每次执行之前先把你的任务告诉我,我审核同意之后就开始执行任务