现在是在实现第一个创新点

目前是打算使用Adaptive Keyframe Sampling for Long Video Understanding这篇论文的仓库作为我们的基底,仓库是AKS-test

我们使用这个仓库为基础实现第一个创新点，并按照[研究背景与创新点](research_context.md)中的步骤逐步推进。

按照一下五步骤:

第一个点关于寻找帧与query之间的联系的措施:

```
冻结：
1. 证据规划使用的大语言模型
2. 文本编码器
3. 视频帧视觉编码器

训练：
1. W_Q、W_K、W_V投影层
2. Cross-Attention层
3. 证据—帧相关性评分头
4. 可选的Query重要性评分头

a方案: 证据查询作为Q,视频帧作为K和V
(每个证据需求主动向整个视频提问: 哪些帧能够回答我)
1. 候选帧生成
2. 问题—选项联合证据规划
3. 证据Query编码
4. 视频帧编码与时间上下文建模
5. 构造Q、K、V
6. 计算Cross-Attention
7. 计算独立证据—帧相关性矩阵
8. 处理“第一次、最后一次、之前、之后”等时间约束
9. 基于证据覆盖、相关性和帧间多样性选择关键帧
10. 输出结构化证据—帧对应关系
```


第一个创新点: 问题—选项联合证据规划与跨模态交互式关键帧检索

第一点分为五步:

1.问题—候选项联合理解以问题子步骤和证据需求构建推理骨架

```
1.输入包括：

Q=问题
模型不直接判断哪个选项正确，也不分别为每个选项找帧，而是分析：

问题属于什么类型；
问题要求定位什么对象、动作、事件或关系；
候选项之间在哪些属性上存在差异；
什么视觉信息能够区分这些差异；
是否需要时间、空间、数量、身份、状态或常识依据；
哪些信息必须同时出现；
哪些信息可以由多个帧联合提供。

例如：

人物第一次进入房间时拿了什么？
联合理解后，模型应识别：

“第一次”要求时间定位；
“进入房间”要求事件边界；
“人物”要求身份保持；
“拿了什么”要求手部和物体局部视觉；
选项决定需要区分的物体类别。
```

2.判别性证据需求生

```
2. 判别性证据需求生成

模型将问题分解为若干证据需求：

E={e1,e2,e3,..,en}

例如：e1=定位目标人物首次进入房间的时刻
e2=确认该时刻出现的是目标人物
e3=寻找目标人物手部无遮挡的画面
e4=提取能够区分候选物体的局部外观特征

这些证据需求应该具有以下性质：

问题相关: 每个查询都直接服务于原始问题。
选项判别: 它不是笼统描述视频，而是针对候选项之间的差异寻找证据。
视觉可检索: 查询应该能映射到视频中的对象、动作、属性、时间点或空间关系。
数量受控: 不能无限分解，否则检索和推理成本会失控。

能够覆盖完整推理链,多个查询合起来应足以支持最终答案。

思路：先让大模型根据问题和候选项推理“为了确定唯一选项，需要从视频中重点寻找哪些信息”，再依据这些要点寻找证据。
```

3.构造证据查询矩阵和视频视觉矩阵

```
3. 构造证据查询矩阵和视频视觉矩阵

将每个证据需求与原问题联合编码：

xi=Etext (Q,O,ei)
堆叠得到证据查询矩阵：X=[x1;x2;…;xm]∈R m×d

视频首先经过粗粒度采样或镜头划分，得到候选帧或片段：F={f1,f2,f3}

每个候选帧编码为视觉表示：=Evision(ft)

得到视觉矩阵：Z=[z1;z2;…;zT]∈R T×d

更细粒度时，每帧还可以保留Patch、对象或区域Token，而不是只有单个全局向量。

这里需要纠正一个概念：

“构造矩阵”是多证据查询和多帧视觉特征的表示形式；QKV跨注意力是这些矩阵进行交互的一种具体机制。
```

4.证据查询与视频帧的跨模态交互

```
4. 证据查询与视频帧的跨模态交互

可以采用以下交互方式之一：

显式证据—帧关系矩阵: Ri,t=g(xi,zt)

其中 g 不必是固定余弦相似度，可以是：
可学习双线性映射；
多层感知机；
融合对象、动作、OCR和时间特征的兼容函数；
图文匹配模型；
帧级判别器。

矩阵：R∈R m×T中的元素 R,i,t表示：第 t 帧能够为第 i 项证据需求提供有效视觉支持的程度。

QKV跨模态交叉注意力

令证据查询作为Query：Q=XWq
视频帧或视觉Token作为Key和Value：K=ZWk,V=ZWv
	
计算：A=softmax(d QK⊤)

然后读取视觉内容：

C=AV
这不仅得到证据—帧关联权重，还会让证据表示吸收相应的视觉信息。

真正的区别不在于“是否计算相似性”，因为QKV中的 QK⊤
 本身也是相关性计算。区别在于：

查询由问题、全部选项和证据需求共同条件化；
查询和视觉特征经过可学习映射；
可以在帧级、片段级或Patch级交互；
问题表示能够读取并吸收视频信息；
可以堆叠多层，完成粗定位到细定位；
检索过程服务于证据获取，而非只做主题匹配。

QD-DETR等工作已经证明，显式地用交叉注意力把查询上下文注入视频表示，可以形成问题依赖的视频表征；因此你们不能把“使用QKV”本身当作创新，创新必须落在问题—选项产生判别性证据Query及其后续使用方式上。
```

5.面向证据覆盖的预算约束选帧

```
5. 面向证据覆盖的预算约束选帧

得到证据—帧关系矩阵后，不能简单从整个矩阵中选择数值最大的K个元素。

最终选择的帧集合：S⊆F,∣S∣≤K

应同时考虑：

证据相关性: 帧是否真正响应某项证据需求。
证据步骤覆盖: 每个必要证据需求是否至少有一帧或一个片段支持。
帧间多样性: 避免选择视觉内容相同的连续帧。
时间完整性: 涉及动作或状态变化时，应保留必要的开始、过程和结束。
候选项判别性: 所选帧是否有助于区分候选项，而不只是与问题主题相关。
视觉预算:最终帧数必须控制在下游大模型可以处理的范围内。

可以将目标抽象为：

Smax[αEvidenceRelevance+βEvidenceCoverage+γTemporalCompleteness+δDiversity−λRedundancy]

这使第一阶段的输出不仅是一组帧，还应包括：

每帧对应的证据需求；
每个关联关系的置信度；
帧或片段的时间位置；
哪些证据需求尚未覆盖；
哪些帧可能同时支持多个要点。

这些信息需要继续传递给第二个创新点，而不是选完帧后丢弃。
```
